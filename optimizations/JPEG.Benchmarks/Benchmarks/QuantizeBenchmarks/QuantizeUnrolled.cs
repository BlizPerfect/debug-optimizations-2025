namespace JPEG.Benchmarks.Benchmarks.QuantizeBenchmarks;

public class QuantizeUnrolled
{
    private static readonly byte[] quantizationMatrix =
    {
        10, 7, 6, 10, 14, 24, 31, 37, 7, 7, 8, 11, 16, 35, 36, 33, 8, 8, 10, 14, 24, 34, 41, 34, 8, 10, 13, 17, 31, 52,
        48, 37, 11, 13, 22, 34, 41, 65, 62, 46, 14, 21, 33, 38, 49, 62, 68, 55, 29, 38, 47, 52, 62, 73, 72, 61, 43, 55,
        57, 59, 67, 60, 62, 59
    };

    public static void Quantize(
        Span<byte> resultY,
        Span<byte> resultCb,
        Span<byte> resultCr,
        Span<float> channelFreqsY,
        Span<float> channelFreqsCb,
        Span<float> channelFreqsCr)
    {
        var index = 0;
        var quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 1;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 2;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 3;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 4;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 5;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 6;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 7;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 8;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 9;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 10;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 11;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 12;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 13;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 14;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 15;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 16;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 17;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 18;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 19;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 20;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 21;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 22;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 23;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 24;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 25;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 26;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 27;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 28;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 29;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 30;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 31;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 32;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 33;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 34;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 35;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 36;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 37;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 38;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 39;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 40;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 41;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 42;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 43;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 44;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 45;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 46;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 47;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 48;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 49;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 50;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 51;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 52;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 53;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 54;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 55;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 56;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 57;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 58;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 59;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 60;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 61;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 62;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
        index = 63;
        quantizationValue = quantizationMatrix[index];
        resultY[index] = (byte)(channelFreqsY[index] / quantizationValue);
        resultCb[index] = (byte)(channelFreqsCb[index] / quantizationValue);
        resultCr[index] = (byte)(channelFreqsCr[index] / quantizationValue);
    }
}